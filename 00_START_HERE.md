---
title: Judges 자료 수집 프로젝트 — 진입점 (세션 인계 마스터)
type: master
updated: 2026-08-01
---

# 00 · START HERE — 새 세션은 이 문서를 먼저 읽는다

> **다른 PC/환경으로 옮겨왔다면 → `00_HANDOFF_다른PC.md`를 먼저 읽을 것** (Drive 계정·GitHub·경로 체크리스트).

홍교수님(공유 폴더 주인)의 조교로서, 사사기 관련 자료를 검색·확보해 업데이트하는 프로젝트다.
목표: **학계 주요 논의를 누락 없이, 가짜 없이** 확보.

**2개 트랙** (둘 다 최종 산출물 = `worklist/download_queue.csv` "새로 다운받을 목록"):
- **Track 1 — Known-missing:** 리스트(3주석서 미보유 2395)에 있으나 파일 없음 → 다운로드 목록.
- **Track 2 — Discovery+Snowball:** 리스트에도 없음 → 웹으로 주제어·카테고리 발굴 → 검증 → 그 자료 참고문헌에서 바운더리 내 추가자료 추출(재귀) → 다운로드 목록.
- 상세: `30_수집전략_2트랙.md`

## ⛔ 절대 규칙 (모든 세션 공통)
1. **대상 드라이브 폴더 `5 Book 3 Judges Resources/` 는 읽기 전용.** 그 안에 쓰기·이동·삭제 금지.
2. 모든 산출물·로그·계획은 **이 `judges report/` 폴더에만** 생성·보관·참조.
3. **가짜 금지**: 서지는 지어내지 않는다. 실재하는 항목만 다룬다. 확인 안 된 값은 `[추정]` 표기.
4. 다음은 **사용자 승인 후에만**: 파일 다운로드, 파일 이동/쓰기, 외부 발송, 삭제.
5. 같은 파일 2회 연속 수정 실패 시 중단·보고.

## 📁 문서 맵 (judges report/)
| 파일 | 역할 |
|---|---|
| `00_START_HERE.md` | (이 문서) 진입점·규칙·현재 상태 |
| `10_진행순서_배치계획.md` | 처리 순서(무거운 순), 배치·청크 분할, 우선순위 규칙 |
| `20_PROGRESS_TRACKER.md` | **세션마다 갱신**하는 진행 상태·세션 로그. 이어받기의 핵심 |
| `30_수집전략_2트랙.md` | Track1(미보유)+Track2(발굴/Snowball) 전략, download_queue 규약 |
| `40_검증방법론.md` | **오류 원인·확정 절차·신뢰등급(A/B/C)**. 정제 시 필수 준거 |
| `OT-검색바운더리.md` | 검색 대상 저널·시리즈 화이트리스트 (필터 기준) |
| `download_dashboard.html` | 다운로드 대장 뷰 (클릭 접근·원본 좌표·받음 체크·A/B/C) |
| `worklist/` | 배치별 정제 CSV + `download_queue.csv`(정본 목록) + `UNRESOLVED.csv` |
| `worklist/monitor_loop_prompt.md` | **감시루프 재등록용 프롬프트·스크립트**(세션 전용이라 매 세션 재등록 필요) |
| `worklist/held_audit.py` | 확보완료 누락 점검(원본 폴더 vs CSV 대조). 자료 추가될 때마다 재실행 |
| `00_COMMANDER.md` | 커맨더 역할·Codex 연동·예약작업·알려진 함정 |
| `70_Codex_UI_요청.md` | 대시보드 UI 작업지시서(WO-001~) |
| `2026-07-13_missing검증_및_수집워크플로우.md` | 데이터 검증 결과 + 7단계 파이프라인 상세 |
| `2026-07-13_현황진단_리포트.md` | 대상 폴더 구조 진단 |

## 🔄 이어받기 절차 (새 세션이 할 일)

### ★ 현재 국면: 정제 완료 → **수집 실행 + 대시보드 운영**(2026-08-01 기준)
정제 23/23 청크 전량 완료(2026-07-20). 지금은 사용자가 실제로 자료를 내려받아 원본 폴더에 넣고, 대시보드에서 확보완료·재검토·확보불가를 표시하면 **커맨더가 그때그때 반영**하는 단계다. 아래 3가지를 세션 시작 시 바로 할 것:

1. **감시루프 재등록** — `worklist/monitor_loop_prompt.md`의 프롬프트를 `CronCreate`로 등록(매시 07분). **세션 전용이라 이전 세션 것은 이미 사라졌다.**
2. **밀린 재검토 처리** — 등록 전에 백엔드 1회 수동 조회로 `pending` 쌓인 것 확인·처리(같은 문서에 스크립트 있음).
3. **신규 확보자료 반영** — 원본 폴더에 파일이 늘었으면 `python3 worklist/held_audit.py` 재실행 → `/Nuri added/` 경로 후보만 반영.

운영 상세·함정은 `00_COMMANDER.md` §3-1과 `20_PROGRESS_TRACKER.md`의 "세션 15" 로그를 볼 것.

### 일반 절차
1. 이 문서로 규칙 확인.
2. `20_PROGRESS_TRACKER.md`를 열어 **마지막 세션 로그**와 **다음 액션**을 확인.
3. (정제 작업이 다시 생기면) `10_진행순서_배치계획.md`에서 배치 작업 정의를 본다.
4. 파이프라인(검증 문서 PART 2) 단계대로 처리 → `worklist/`에 CSV 산출.
5. 끝나면 `20_PROGRESS_TRACKER.md`에 결과·다음 액션을 **append**.

## 📌 현재 상태 (2026-08-01)
- [x] 데이터 검증 완료 (계보·정합성·품질리스크 파악)
- [x] 검색 바운더리 확보 (`OT-검색바운더리.md`)
- [x] 진행 순서·배치 계획 수립
- [x] **정제(단계1) 전량 완료 (2026-07-20)** — 23/23 청크, 원행 2395건 전건 처리
- [x] 대시보드 운영 체계 구축 — GitHub Pages + Google Sheets 백엔드(재검토·확보완료·확보불가·자료종류 재분류 동기화)
- [ ] **실제 자료 수집 진행 중** ← 현재 여기
- `download_queue.csv` **2286항목** (QUEUED 2141 / 확보완료 145) · UNRESOLVED 35 · C등급 113

## 다음 액션 (한 줄)
→ 세션 시작 시 **감시루프 재등록 + 밀린 재검토 처리 + 신규 확보자료 반영**(위 "이어받기 절차" 참조). 이후는 사용자 지시에 따라 수집 반영·대시보드 개선.
